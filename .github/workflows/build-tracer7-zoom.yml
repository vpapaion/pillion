name: Build Pillion Zoom Joystick APK

on:
  workflow_dispatch:
  push:
    branches:
      - agent/tracer7-display-zoom

permissions:
  contents: write

jobs:
  android:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Apply pending joystick feature source patch
        run: |
          if [ -f .github/joystick-feature.patch.gz ]; then
            gzip -dc .github/joystick-feature.patch.gz | git apply --check
            gzip -dc .github/joystick-feature.patch.gz | git apply
            rm .github/joystick-feature.patch.gz
          fi

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 35
        run: sdkmanager 'platforms;android-35' 'build-tools;35.0.0'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build and test
        run: |
          chmod +x gradlew
          ./gradlew --stacktrace :composeApp:testDebugUnitTest :composeApp:assembleDebug

      - name: Publish verified source changes
        run: |
          if ! git diff --quiet || [ -n "$(git ls-files --others --exclude-standard)" ]; then
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git add -A
            git diff --cached --check
            git commit -m 'feat(android): control crop with Tracer joystick [skip ci]'
            git push origin HEAD:${{ github.ref_name }}
          fi

      - name: Name APK
        run: cp composeApp/build/outputs/apk/debug/composeApp-debug.apk Pillion-Zoom-Tracer7-Joystick.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Pillion-Zoom-Tracer7-Joystick
          path: Pillion-Zoom-Tracer7-Joystick.apk
          if-no-files-found: error
          retention-days: 30
          # This artifact is the real-bike test build for Tracer joystick crop controls.
